const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav');
function closeMenu(){nav.dataset.open='false';menu.setAttribute('aria-expanded','false');menu.textContent='القائمة'}
menu?.addEventListener('click',()=>{const open=nav.dataset.open!=='true';nav.dataset.open=String(open);menu.setAttribute('aria-expanded',String(open));menu.textContent=open?'إغلاق':'القائمة'});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav?.dataset.open==='true'){closeMenu();menu.focus()}});
document.addEventListener('click',e=>{if(nav?.dataset.open==='true'&&!e.target.closest('.site-header'))closeMenu()});
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));let count=0;document.querySelectorAll('[data-category]').forEach(card=>{card.hidden=button.dataset.filter!=='all'&&card.dataset.category!==button.dataset.filter;if(!card.hidden)count++});const status=document.getElementById('filter-status');if(status)status.textContent=`عدد الأعمال المعروضة: ${count}`}));
const dialog=document.querySelector('.lightbox'),photos=[...document.querySelectorAll('[data-photo]')];let current=0,returnTo;
function showPhoto(i){current=(i+photos.length)%photos.length;const b=photos[current],img=dialog.querySelector('img');img.src=b.dataset.photo;img.alt=b.dataset.caption;dialog.querySelector('[data-count]').textContent=`${current+1} / ${photos.length}`;dialog.querySelector('[data-caption]').textContent=b.dataset.caption}
photos.forEach((b,i)=>b.addEventListener('click',()=>{returnTo=b;showPhoto(i);dialog.showModal()}));
dialog?.querySelector('[data-close]').addEventListener('click',()=>dialog.close());dialog?.querySelector('[data-next]').addEventListener('click',()=>showPhoto(current+1));dialog?.querySelector('[data-prev]').addEventListener('click',()=>showPhoto(current-1));
dialog?.addEventListener('close',()=>returnTo?.focus());dialog?.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});dialog?.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();showPhoto(current+1)}if(e.key==='ArrowRight'){e.preventDefault();showPhoto(current-1)}});
const form=document.querySelector('.brief-form');form?.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const d=new FormData(form),message=[`السلام عليكم ${form.dataset.office}`,`الاسم: ${d.get('name')}`,`الخدمة المطلوبة: ${d.get('service')}`,`مدينة المشروع: ${d.get('city')}`,d.get('details')?`تفاصيل المشروع: ${d.get('details')}`:''].filter(Boolean).join('\n');const status=document.querySelector('.form-status');status.hidden=false;status.querySelector('pre').textContent=message;const link=status.querySelector('[data-send]');link.href=form.dataset.wa.includes('/message/')?form.dataset.wa:form.dataset.wa+'?text='+encodeURIComponent(message);status.querySelector('[data-copy]').onclick=async()=>{try{await navigator.clipboard.writeText(message);status.querySelector('[data-copy]').textContent='تم نسخ الطلب'}catch{status.querySelector('[data-copy]').textContent='يمكنك تحديد النص ونسخه يدوياً'}};status.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'nearest'});});
document.querySelector('[data-print]')?.addEventListener('click',()=>window.print());
if(form){const requested=new URLSearchParams(location.search).get('service'),select=form.querySelector('select');if(requested&&[...select.options].some(o=>o.value===requested))select.value=requested}
